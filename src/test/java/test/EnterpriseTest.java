package test;

import org.junit.Test;

public class EnterpriseTest {
    @Test
    public void addPosition() {
        // Map<String,String> map=new HashMap<>();
        // map.put("city", "henan");
        // map.put("salary","");
        // Position position=BeanProcessor.newInstance(Position.class, map);
        // EnterpriseService.addPosition(2, position);
    }
    @Test
    public void updateEnterprise(){
//        Enterprise enterprise=EnterpriseService.getEnterprise(2);
//        enterprise.setName("ssss");
//        EnterpriseService.updateEnterprise(enterprise);
    }
}
